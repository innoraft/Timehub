<?php

function integrate_github_menu() {
  $items = array();  
  
  $items['github_info'] = array(
    'title' => t('Github Info'),
    'page callback' => 'github_info',
    'page argument' => array(1),
    'access callback' => TRUE,
    'type' => MENU_NORMAL_ITEM
  ); 
  
  return $items;
}

function github_info($values = ''){
  $payload = json_decode($_POST[$values]);
  if ($payload->ref === 'refs/heads/aman-correction-code'){
    $url = $payload->repository->url;
    dpm($url);
  }
  //watchdog('integrate_github', $message);
  return "hell";
}
